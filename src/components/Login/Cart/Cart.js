import React from 'react';
import Gallery from '../../Gallery/Gallery';

const Cart = (props) => {
    console.log(props)
    return (
        <div>
            <Gallery></Gallery>
        </div>
    );
};

export default Cart;